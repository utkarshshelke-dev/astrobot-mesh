[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    live_bq: requires real BigQuery credentials
    live_agent: requires LLM API access
filterwarnings =
    ignore::DeprecationWarning
asyncio_mode = auto
